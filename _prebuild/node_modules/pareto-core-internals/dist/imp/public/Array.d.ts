import * as pt from "pareto-core-types";
export declare class Array<T> implements pt.Array<T> {
    private data;
    constructor(data: T[]);
    map<NT>($v: (entry: T) => NT): Array<NT>;
    asyncMap<NT>($v: ($: T) => pt.AsyncValue<NT>): pt.AsyncValue<pt.Array<NT>>;
    __forEach($i: ($: T) => void): void;
    __getLength(): number;
    __getElementAt(index: number): T;
}
