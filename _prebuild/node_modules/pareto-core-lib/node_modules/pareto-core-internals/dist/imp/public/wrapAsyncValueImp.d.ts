import * as pt from "pareto-core-types";
import { Execute } from "../types/Execute";
export declare class AsyncValueClass<T> implements pt.AsyncValue<T> {
    private execute;
    constructor(execute: Execute<T>);
    map<NT>($v: ($: T) => AsyncValueClass<NT>): pt.AsyncValue<NT>;
    __execute($i: ($: T) => void): void;
}
/**
 * returns an {@link AsyncValue }
 * @param execute the function that produces the eventual value
 * @returns
 */
export declare function wrapAsyncValueImp<T>(execute: Execute<T>): pt.AsyncValue<T>;
