import { AsyncValue } from "./AsyncValue";
export interface Array<T> {
    map<NT>($v: ($: T) => NT): Array<NT>;
    asyncMap<NT>($v: ($: T) => AsyncValue<NT>): AsyncValue<Array<NT>>;
    __forEach($i: ($: T) => void): void;
    __getLength(): number;
    __getElementAt(index: number): T;
}
